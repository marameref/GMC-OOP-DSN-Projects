# Data Structures Lab: Queues & Heaps

## 📂 Project Files
- **index.html**: A visual dashboard to trigger and see test results.
- **queue_structures.js**: Contains `ArrayQueue` (fixed) and `LinkedListQueue` (dynamic) logic.
- **priority_queues.js**: Implements a `MinHeapPQ` for efficient $O(\log n)$ priority management.

## 🛠️ Testing Logic
1. Open `index.html` in any browser.
2. Click the **Run Test** buttons.
3. Observe the terminal output for correctness:
    - Queues should follow **FIFO** order.
    - Min-Heap should always return the **lowest number** first.

## 📈 Learning Outcomes
- Understanding **Circular Buffers** in arrays.
- Managing **Nodes and Pointers** in Linked Lists.
- Mastering **Heapify** (Bubble-up/down) operations.